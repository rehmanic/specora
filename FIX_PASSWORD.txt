# 🔧 Fix Password Mismatch

You're in psql as postgres user. Run these commands:

```sql
-- Change specora user password to match your .env file
ALTER USER specora WITH PASSWORD '1038_specora_dev_2025_2948';

-- Verify
\du specora

-- Exit
\q
```

After this, the password will match what's in your `backend/.env` file.

Then test with:
```bash
cd backend
node setup-and-seed.js
```
